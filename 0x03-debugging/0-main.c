#include "main.h"

/**
 * main - Entry point. Tests function that prints text based
 *	on whether an integer is positive or negative
 * Return: Always 0 (Success)
 */

int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}

